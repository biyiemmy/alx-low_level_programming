#include "main.h"

/**
 * _is upper -> this is a function that checks for uppercase character
 * @c: an integer parameter
 *
 * Return: something but success
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
